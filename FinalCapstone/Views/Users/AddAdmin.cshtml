@model AddAdminViewModel

@{
    ViewData["Title"] = "Add Admin";
}

<h2>Add Admin</h2>

@using (Html.BeginForm("AddAdmin", "Users", FormMethod.Post))
{
    <section id="one" class="wrapper style2">
        <div class="inner">
            <div class="row4">
                @Html.TextBoxFor(m => m.EmailAddress, new { placeholder = "Email" })
                @Html.ValidationMessageFor(m => m.EmailAddress)
            </div>
        </div>
        <div>
            <button class="button button1">Make User an Admin</button>
        </div>
    </section>
}